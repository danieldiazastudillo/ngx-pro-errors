import { ProErrorDirective } from './pro-error.directive';

describe('ProErrorDirective', () => {
  it('should create an instance', () => {
    const directive = new ProErrorDirective();
    expect(directive).toBeTruthy();
  });
});
