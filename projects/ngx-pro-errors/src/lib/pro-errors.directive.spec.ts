import { ProErrorsDirective } from './pro-errors.directive';

describe('ProErrorsDirective', () => {
  it('should create an instance', () => {
    const directive = new ProErrorsDirective();
    expect(directive).toBeTruthy();
  });
});
